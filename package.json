{
    "scripts": {
      "serve": "serve build_output -l 3000",
      "open": "open http://localhost:3000",
      "build": "thumbsup --config ./config.json",
      "clean": "rm -rf build_output",
      "flatten": "sh generate_wallpaper_folder.sh",

      "rebuild": "npm run clean && npm run build",
      "dev": "npm run build -- --watch && npm run serve && npm run open"
    }
  }